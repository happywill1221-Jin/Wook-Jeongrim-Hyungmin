# 나의 에세이 홈페이지

에세이를 작성하고, 보관하고, 공개하는 나만의 홈페이지입니다.  
Next.js 14 App Router + TypeScript로 제작되었습니다.

---

## 🚀 로컬 실행

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:3000` 접속

---

## ✏️ 사용법

| 화면 | URL |
|------|-----|
| 독자용 (공개 에세이만) | `/` |
| 관리자 모드 (전체 에세이) | `/?admin=1` |
| 새 에세이 작성 | `/editor/new` |
| 에세이 수정 | `/editor/{id}` |

> **보안 팁**: 실제 배포 시 `?admin=1` 대신 NextAuth 등으로 로그인 기능을 추가하세요.

---

## ☁️ Vercel 배포 방법

### 1단계 - GitHub에 올리기

```bash
git init
git add .
git commit -m "나의 에세이 홈페이지"
git remote add origin https://github.com/YOUR_USERNAME/essay-homepage.git
git push -u origin main
```

### 2단계 - Vercel 배포

1. [vercel.com](https://vercel.com) 접속 → GitHub 로그인
2. "New Project" → 위에서 만든 저장소 선택
3. "Deploy" 클릭 → 완료!

배포 후 `yourname.vercel.app` 주소로 누구나 접근 가능합니다.

---

## ⚠️ Vercel 배포 시 데이터 저장 주의사항

현재는 **로컬 JSON 파일**(`data/essays.json`)에 저장합니다.  
Vercel 서버리스 환경에서는 파일 시스템이 휘발성이라 **데이터가 사라질 수 있습니다**.

### 해결 방법 (권장)

**방법 1 - Vercel KV (Redis)** — 가장 간단
```bash
npm install @vercel/kv
```
Vercel 대시보드에서 KV 데이터베이스 생성 후 `lib/essays.ts`의 파일 읽기/쓰기 부분을 KV로 교체.

**방법 2 - PlanetScale / Supabase (MySQL/PostgreSQL)**  
규모가 커지면 관계형 DB 사용 권장.

**방법 3 - 로컬 전용으로만 사용**  
`npm run dev`로 내 컴퓨터에서만 사용하면 JSON 파일로도 충분합니다.

---

## 📁 프로젝트 구조

```
essay-homepage/
├── app/
│   ├── page.tsx              # 메인 목록 페이지
│   ├── layout.tsx            # 루트 레이아웃
│   ├── globals.css           # 전역 스타일
│   ├── essays/[id]/page.tsx  # 에세이 읽기 페이지
│   ├── editor/
│   │   ├── new/page.tsx      # 새 에세이 작성
│   │   └── [id]/page.tsx     # 에세이 수정
│   └── api/essays/
│       ├── route.ts          # GET 목록, POST 생성
│       └── [id]/route.ts     # GET, PUT, DELETE
├── components/
│   ├── Header.tsx
│   ├── EssayList.tsx
│   ├── EssayCard.tsx
│   └── EditorForm.tsx
├── lib/
│   └── essays.ts             # 데이터 로직
└── data/
    └── essays.json           # 에세이 데이터 (자동 생성)
```
